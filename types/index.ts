export type LoginData = {
  email: string;
  password: string;
};

export type RegisterData = {
  name: string;
  email: string;
  password: string;
};

export type Sofa = {
  id: string;
  name: string;
  description: string;
  cost: number;
  sofaType: string;
  imgurl: string;
};
